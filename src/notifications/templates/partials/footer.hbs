<div class="footer">
    <div class="footer-links">
        <a href="{{helpCenterUrl}}" class="footer-link">{{common.helpCenter}}</a>
        <a href="{{contactUrl}}" class="footer-link">{{common.contact}}</a>
        <a href="{{privacyPolicyUrl}}" class="footer-link">{{common.privacy}}</a>
        <a href="{{termsUrl}}" class="footer-link">{{common.terms}}</a>
    </div>
    <p>{{{common.footer}}}</p>
    <p class="copyright">{{common.autoEmailNotice}}</p>
</div>